#include <stdio.h>

void left_shift_one(char *s,int n){
	assert(s!=NULL);
	char t = *s; //保持第一个字符

	while(i++<n){
		*(s+i-1)=*();
	}
}
int main(int argc, char *argv[])
{
	printf("Hello, world\n");
	
	return 0;
}
